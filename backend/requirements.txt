# Local development + testing dependencies. This is NOT what gets bundled
# into Lambda deployment packages — see requirements-lambda.txt for that,
# and backend/scripts/package_lambdas.sh for which function gets which
# subset of it.

boto3>=1.34
opensearch-py>=2.6
pypdf>=4.0
python-docx>=1.1
pytest>=8.0
flake8>=7.0
